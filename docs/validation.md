# 已有证据与仍待验证的内容

## 记录运行

2026-09-12 23:10:30 至 09-13 14:15:41（UTC+8），一次正式 Calculate 完成 100000 步、0.01 s；墙钟时间 54311.75 s。准备阶段的错误修复、时间零点读回与正式推进分开记录。正式开始后未在完整 transcript 中发现 Case/Data 再读取。

全部时间步有 Fluent 收敛标记，内迭代上限命中 0 次；既有检查记录的每步内迭代最小 1、最大 174、均值 2.48652。残差标记不能证明物理正确。

最终 CSV 有 30000 行、39 列、1170000 个数值，既有和本轮离线检查均检查有限性。历史有 100001 行，步号连续 0–100000。已有原生 HDF 审计记录 110 个数值 results 数据集、2364010 个值有限，最终 Data 与最后同一步 checkpoint 数值相同。这些数字描述历史审计范围，不能自动当作路径清理版已重新检查的结论。

## 公开原生文件的检查状态

`native/final-100000.cas.h5` 与 `native/final-100000.dat.h5` 是保留完整设置和终态数值的路径清理副本，不是移除 UDF 挂接的查看专用版本。同目录配有四个 UDF 源文件，查看完整 UDF 模型或继续求解前须在实际使用的 Fluent 环境手动 Build/Load `libfuel`；没有复用原机器的 DLL。原 Case 的 `udf/compile/files` 列表为空，放入同目录不保证自动编译。

路径清理采用 HDF5 对象语义复制，只替换文本路径。需要分别核对对象/设置保留、每个数值数据集及其属性、允许变更的文本对象，以及软件实际读取。文件布局和路径文本可能使整文件哈希改变，不能用“与原文件哈希不同”直接判断数值被改，也不能用“数据集相同”直接宣称软件读取成功。

HDF5 逐对象核查已通过：路径变更限于 Case 的 5 行设置及 Data 的 1 行路径历史设置；其余 Case 40 个数据集、Data 116 个数据集与原件逐字节相同，全部对象属性保持一致。Case 中的 29 个数值数据集（422454 个值）及 Data 中的 110 个数值数据集（2364010 个值）均保持原值且有限。两处变更的文本数据集也逐行核对，只有旧目录前缀被替换为相对路径；物性、边界、数值方法和 UDF 挂接未变。详见[原件对比记录](../verification/native_files_check.json)。可选工具 `python tools/verify_native.py`（需要 h5py/NumPy）能核对当前文件是否仍与这份记录一致，不启动 Fluent。

**Fluent 2026 R1 实际读取状态：`NOT_VERIFIED_RUNTIME_BLOCKED`。** Windows 代码完整性策略在启动阶段拦截 Fluent 的 `Qt5Widgets.dll`（事件 3077/3033），因此本次公开副本读取测试未能开始；没有关闭或绕过该策略。这是运行环境阻断，不是 Case/Data 读取成功或文件无效的证据，见[读取测试记录](../verification/native_read_attempt.json)。原私有配套文件以前读取成功，不能作为清理后的公开副本已通过的证明。

后续读取测试应记录是否成功加载配套 Case/Data、本机编译与挂接状态、读回单元数/步数/物理时间及错误信息。其范围是文件可读性与保存状态检查，不是重新完成全程计算或新增物理验证。Fluent 2024 R2 尚未实测。

## 守恒与半径

基于监测历史的最后 1000 步 BDF2 条件性守恒余差，相对累计入口质量，CO₂ 约 1.22897×10⁻⁷%、总水约 0.00683221%、总质量约 0.000634391%。缺少完整边界扩散通量和局部反流信息，尚未证明严格全部离散通量闭合。

原始最大诊断半径约 780.768 μm，来自极小液体载荷与滴数的矩比尾部；r≥1 μm 的 88 个单元在原 HDF 审计中只占正液态库存约 2.11×10⁻¹⁸。半径参与增长计算，不能仅凭末态库存很小就断言整个过程从未受影响。

L>10⁻¹²、N>0 的显示筛选保留 15035 个单元，有效半径约 7.227–129.792 nm。这是显示规则。负 UDS 原值、风险标志和未筛选原始半径都保留在 CSV 中。

原始末行 history 和最终快照的部分 UDM 极值略有不同，最大半径约差 0.07617%。诊断刷新和报告时序是合理解释，但未独立证明完整因果。因此 history 与末态不能被描述成所有字段逐值相同。

## 公开版本的测试层级

|层级|可用证据|不能推出|
|---|---|---|
|原始完整运行|经过脱敏的 transcript、完整数值 history、终态 CSV、来源哈希|模型已有实验验证|
|原代码实现测试|核心与准一维测试来源、历史结果；本轮测试另有日期和范围|完整 Fluent 挂接/迭代均正确|
|网格重建|与原 .msh 字节一致|网格足够细或网格无关|
|路径清理的原生文件|全部 158 个数据集逐集核对；仅 2 个文本数据集中的路径前缀改变，其他数据集及全部属性不变|清理版已经被 Fluent 成功读取，或与原文件逐字节相同|
|Fluent 2026 R1 读取检查|`NOT_VERIFIED_RUNTIME_BLOCKED`；代码完整性策略阻断启动，尚未读取公开副本|公开副本已读取成功或读取失败；已经重算、移机验证或物理验证|
|手动使用与设置核对|配套 Case/Data、四个 UDF 源文件及操作说明；没有自动准备程序|仅阅读说明即可证明目标安装中全部挂接可用|
|公开数据工具|字段、有限性、连续记录、掩膜、文件/来源校验|严格守恒或物理准确|
|Windows 2024 R2|尚未实机测试|目标版本已通过读取、编译、续算或后处理验证|

核查记录位于 [verification](../verification/)，查看保存结果见 [使用教程](tutorial.md)，模型设置及挂接见 [手动设置核对](manual_setup.md)。AI 辅助代码和整理工作的存在不会改变这些证据边界。

后续需要独立物理基准、网格/时间步/内迭代敏感性，液相储能与动量、混合气传质、物性适用范围，以及近空群/负值/限幅影响的核查。图形外观与“完整算完”都不能替代这些工作。
