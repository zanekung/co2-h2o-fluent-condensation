# 贡献和复核

欢迎改进可复现性、测试和科学模型。变更应说明它属于物理模型、数值控制、可移植性还是后处理显示，避免混在一起。

提供自己的软件版本、输入和日志；保留现有参考数据及哈希，不用新结果覆盖后仍沿用旧来源声明。若修正模型，请在独立运行目录生成新结果，并说明变化对守恒、限幅、液滴群及关键输出的影响。

本包使用配套原生 Case/Data 和手动设置说明，不提供自动准备程序。查看已有结果见 [使用教程](docs/tutorial.md)，编译配套 UDF 和核对挂接见 [手动设置说明](docs/manual_setup.md)。仅查看终态时不要重新初始化或计算；需要复算时使用独立目录，并记录实际 Fluent 版本。

修改原生 HDF5 的路径信息时，按对象结构处理，只允许明示的文本路径变更，保留完整设置和数值；提交逐集数值/属性对比及路径变更清单。整体哈希会因路径或存储布局变化而不同，不能把它当作数值相同的替代证据。软件实际读取、UDF 本机编译/挂接、继续计算和完整重算是不同测试，应分别报告，尤其不能把 2026 R1 的结果写成 2024 R2 已测试。

离线基础检查为 `python tools/verify_case.py` 和 `python tools/analyze_results.py`。有编译环境可运行核心测试。Fluent 端到端测试需要单独记录，不把静态检查替代为 solver pass。

不得提交软件安装包、第三方库/字体/文献全文、未授权稿件、许可证、连接凭据或个人路径。按照项目相应许可贡献自己有权许可的内容，并保留引用和第三方来源。
